# VSCode の使い方

```text
このカリキュラムで使う VSCode（Visual Studio Code）の基本操作をまとめたリファレンス
```

# 目次
- [VSCode の使い方](#vscode-の使い方)
- [目次](#目次)
- [インストール](#インストール)
- [拡張機能を入れる](#拡張機能を入れる)
- [pygame フォルダを開く](#pygame-フォルダを開く)
- [Python ファイルの実行方法](#python-ファイルの実行方法)
  - [ターミナルから実行する（推奨）](#ターミナルから実行する推奨)
  - [実行ボタンから実行する](#実行ボタンから実行する)
  - [作業ディレクトリに関する注意](#作業ディレクトリに関する注意)
- [Python インタープリタの選択](#python-インタープリタの選択)
- [デバッグの基本](#デバッグの基本)
- [便利なショートカット](#便利なショートカット)
- [よくあるトラブル](#よくあるトラブル)

---

# インストール

公式サイト( https://code.visualstudio.com/ )からダウンロードしてインストールする。

| OS | 手順 |
| --- | --- |
| Mac | `.zip` をダウンロードして展開し、`Visual Studio Code.app` を「アプリケーション」フォルダに移動 |
| Windows | `.exe` をダウンロードして実行。インストーラーの指示に従う |

インストール後、アプリを起動して以下のような画面が表示されれば完了。

---

# 拡張機能を入れる

左側のアクティビティバーから拡張機能アイコン（四角が4つ並んだアイコン）を開き、以下を検索してインストールする。

| 拡張機能 | 提供元 | 役割 |
| --- | --- | --- |
| Python | Microsoft | Python の実行・デバッグ・補完機能を追加する |
| Pylance | Microsoft | Python の高度な補完・型チェック（Python 拡張機能と一緒に入ることが多い） |
| Japanese Language Pack | Microsoft | メニューやメッセージを日本語化する（任意） |

> 「Python」拡張機能さえ入れれば、このカリキュラムを進めるのに必要な機能はほぼ揃う。

---

# pygame フォルダを開く

**重要: 個別のレッスンフォルダではなく、このリポジトリのルート（`pygame` フォルダ）を開く。**

```
メニュー: ファイル → フォルダーを開く...
```

`004_draw_images` や `016_battle_game` のような**個別のレッスンフォルダを直接開かない**こと。このカリキュラムのサンプルは「`pygame` フォルダを開いた状態で、そこを基準に実行する」ことを前提にパスが書かれている（詳しくは後述の[作業ディレクトリに関する注意](#作業ディレクトリに関する注意)を参照）。

正しく開けると、左側の「エクスプローラー」に `000_setup` や `001_window` などのフォルダが並んで表示される。

---

# Python ファイルの実行方法

## ターミナルから実行する（推奨）

`` Ctrl + ` ``（バッククォート）でVSCode内蔵のターミナルを開く。ターミナルの操作自体は [003_linux_command](003_linux_command/README.md) を参照。

```bash
# pygame フォルダを開いた状態でターミナルを開くと、そこがカレントディレクトリになる
python 004_draw_images/example/ex01.py
```

このように**ファイルパスを指定して実行する**方法が、このカリキュラムでは最も安全。

## 実行ボタンから実行する

エディタ右上の ▷（三角形）ボタンで実行。

## 作業ディレクトリに関する注意

このカリキュラムのサンプルコードは、画像や音声ファイルを次のように読み込んでいる。

```python
# 004_draw_images/example/ex01.py の場合
img1 = pg.image.load("004_draw_images/images/car.png")
```

これは「**`pygame` フォルダを作業ディレクトリ（カレントディレクトリ）として実行する**」ことを前提にしたパスの書き方。そのため、実行するときの作業ディレクトリがズレると `FileNotFoundError` になる。

| 実行方法 | 作業ディレクトリ | 結果 |
| --- | --- | --- |
| `pygame` フォルダを開いてターミナルから実行 | `pygame/` | ✅ 正しく動く |
| VSCode の ▷ 実行ボタン（`pygame` フォルダを開いている場合） | 通常は `pygame/` | ✅ 正しく動く |
| 個別レッスンフォルダ（例: `004_draw_images`）を開いて実行 | `004_draw_images/` | ❌ 画像が見つからずエラーになる |
| 「Code Runner」拡張機能（デフォルト設定） | ファイルのあるフォルダ | ❌ 画像が見つからずエラーになる |

> **「Code Runner」拡張機能を使う場合は要注意。** デフォルトでは実行するファイルのあるフォルダを作業ディレクトリにしてしまうため、このカリキュラムのサンプルとは相性が悪い。使う場合は設定で `code-runner.fileDirectoryAsCwd` を無効にするか、素直にターミナルから `python <パス>` で実行すること。

エラーが出たら、まず「`pygame` フォルダを開いているか」「ターミナルの現在地（`pwd`）が `pygame` フォルダになっているか」を確認する。

---

# Python インタープリタの選択

複数の Python がインストールされている環境では、VSCode がどの `python` を使うか選ぶ必要がある。

```
コマンドパレット（Cmd/Ctrl + Shift + P）→ "Python: Select Interpreter" → 使用する Python を選択
```

ウィンドウ下部のステータスバーにも現在選択中のインタープリタが表示される。`000_setup` で確認した `python --version` / `which python`（Mac）・`where python`（Windows）と同じものを選ぶとよい。

---

# デバッグの基本

コードの左端の行番号の左をクリックすると赤い点（ブレークポイント）が付き、そこで処理が一時停止する。

| 操作 | ショートカット | 内容 |
| --- | --- | --- |
| デバッグ開始 | `F5` | ブレークポイントまで実行して停止する |
| ステップオーバー | `F10` | 1行ずつ実行する（関数の中には入らない） |
| ステップイン | `F11` | 1行ずつ実行する（関数の中にも入る） |
| 続行 | `F5` | 次のブレークポイントまで実行を再開する |
| 停止 | `Shift + F5` | デバッグを終了する |

停止中は、変数の中身をエディタ上でホバー表示したり、左側の「変数」パネルで確認できる。`print()` を大量に埋め込む代わりに、怪しい箇所にブレークポイントを置いて変数の値を確認する使い方がおすすめ。

---

# 便利なショートカット

| 操作 | Mac | Windows / Linux |
| --- | --- | --- |
| ターミナルの表示/非表示 | `` Ctrl + ` `` | `` Ctrl + ` `` |
| パネルの表示/非表示（ターミナル欄全体） | `Cmd + J` | `Ctrl + J` |
| コマンドパレット | `Cmd + Shift + P` | `Ctrl + Shift + P` |
| ファイルを開く | `Cmd + P` | `Ctrl + P` |
| 保存 | `Cmd + S` | `Ctrl + S` |
| 元に戻す | `Cmd + Z` | `Ctrl + Z` |
| やり直す | `Cmd + Shift + Z` | `Ctrl + Y` |
| 切り取り | `Cmd + X` | `Ctrl + X` |
| コピー | `Cmd + C` | `Ctrl + C` |
| 貼り付け | `Cmd + V` | `Ctrl + V` |
| すべて選択 | `Cmd + A` | `Ctrl + A` |
| ファイル内検索 | `Cmd + F` | `Ctrl + F` |
| ファイル内置換 | `Cmd + Option + F` | `Ctrl + H` |
| サイドバーの表示/非表示 | `Cmd + B` | `Ctrl + B` |
| 新規ファイル作成 | `Cmd + N` | `Ctrl + N` |
| すべて保存 | `Cmd + Option + S` | `Ctrl + K S` |
| 行をコメントアウト | `Cmd + /` | `Ctrl + /` |
| 行の複製 | `Shift + Option + ↓` | `Shift + Alt + ↓` |
| 行の削除 | `Cmd + Shift + K` | `Ctrl + Shift + K` |
| 複数カーソル（縦に追加） | `Cmd + Option + ↓` | `Ctrl + Alt + ↓` |
| 同じテキストを複数選択（1つずつ追加） | `Cmd + D` | `Ctrl + D` |
| 同じテキストを複数選択（一括） | `Cmd + Shift + L` | `Ctrl + Shift + L` |
| 全体検索 | `Cmd + Shift + F` | `Ctrl + Shift + F` |
| インデント整形 | `Shift + Option + F` | `Shift + Alt + F` |
| 定義へジャンプ | `F12` | `F12` |
| 画面の拡大 | `Cmd + =` | `Ctrl + =` |
| 画面の縮小 | `Cmd + -` | `Ctrl + -` |
| 開いているファイル（プログラム）の切り替え | `Ctrl + Tab` | `Ctrl + Tab` |
| 開いているファイルを閉じる | `Cmd + W` | `Ctrl + W` |
| ウィンドウを最小化 | `Cmd + M` | `Win + ↓` |
| ウィンドウを最大化 | `Option + クリック（緑ボタン）` | `Win + ↑` |

---

# よくあるトラブル

| 症状 | 原因 | 対処 |
| --- | --- | --- |
| `FileNotFoundError` で画像が読み込めない | 作業ディレクトリが `pygame` フォルダになっていない | [作業ディレクトリに関する注意](#作業ディレクトリに関する注意) を参照 |
| `ModuleNotFoundError: No module named 'pygame'` | 選択中の Python に pygame がインストールされていない | [Python インタープリタの選択](#python-インタープリタの選択) で正しい Python を選ぶ、または `000_setup` を参照して pygame を再インストール |
| 実行してもウィンドウが一瞬で閉じる | エラーで強制終了している | ターミナルに表示されたエラーメッセージ（一番下の行）を確認する |
| 日本語コメントが文字化けする | ファイルの文字コードが UTF-8 になっていない | 右下のステータスバーの文字コード表示をクリックし「エンコード付きで保存」→ UTF-8 を選ぶ |
| 保存しても実行結果が変わらない | 実行しているファイルと編集しているファイルが違う（同名ファイルが複数ある） | ターミナルに表示された実行パスを確認する |
