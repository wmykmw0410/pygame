"""Q3. 単位変換モジュール

単位変換関数をまとめた converter.py モジュールを作成し、3パターンのインポート方法で呼び出す

step1. converter.py に以下の関数を作る
       ・cm_to_inch(cm)            → cm をインチに変換（1インチ = 2.54cm）
       ・kg_to_lb(kg)              → kg をポンドに変換（1ポンド = 0.4536kg）
       ・celsius_to_fahrenheit(c)  → 摂氏を華氏に変換（F = C * 9/5 + 32）

step2. import converter で呼び出す（use_step2.py を作成）
step3. import converter as cv で別名呼び出す（use_step3.py を作成）
step4. from converter import ... で直接インポートする（use_step4.py を作成）

※ converter.py、use_step2.py、use_step3.py、use_step4.py をこのフォルダに作成する
"""
